import { ShoppingCart } from 'lucide-react'
import React from 'react'

const CartIcon = ({ size = 24, color = '#654321' }) => {
	return <ShoppingCart size={size} color={color} />
}

export default CartIcon
